  <% @tasks.each do |task| %>
  <p>
    <%= task.done_checkbox %> <%= link_to task.name, task_path(task) %> - due for <%= task.readable_due_date %>
  </p>
  <% end %>

<p><%= link_to "New task", new_task_path %></p>
